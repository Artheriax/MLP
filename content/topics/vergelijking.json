{
  "id": "vergelijking",
  "chapter": "ch4",
  "title": "LR vs. SVM & de SGDClassifier",
  "intro": null,
  "keyPoints": [
    "Grote data of kansen nodig → <strong>logistic regression</strong>; heldere marge op weinig data → <strong>SVM</strong>.",
    "Belangrijkste hyperparameters: <strong>C</strong> (beide), <strong>penalty</strong> (LR), <strong>gamma</strong> (RBF-SVM).",
    "<code>SGDClassifier</code> bewijst het inzicht: LR en SVM zijn hetzelfde model — <strong>alleen de loss verschilt</strong> (log vs. hinge).",
    "Schaal features vóór SVM's, en gebruik een <code>Pipeline</code> om leakage te voorkomen."
  ],
  "level": 3,
  "sections": [
    {
      "title": "Pros & cons naast elkaar",
      "blocks": [
        {
          "type": "text",
          "html": "Beide zijn lineaire classifiers en kunnen met kernels. Scherpste verschil: bij logistic regression be&iuml;nvloeden <strong>alle</strong> punten de fit, bij een SVM alleen de <strong>support vectors</strong>. En bij conventie betekent &ldquo;SVM&rdquo;: hinge loss <em>plus</em> L2-regularisatie."
        },
        {
          "type": "table",
          "headers": [
            "Aspect",
            "Logistic Regression",
            "SVM"
          ],
          "rows": [
            [
              "Type",
              "lineaire classifier",
              "lineaire classifier (hinge + L2)"
            ],
            [
              "Kernels",
              "kan, maar minder gebruikelijk",
              "gebruikelijk — snel bij weinig support vectors"
            ],
            [
              "Kansen",
              "natuurlijk (predict_proba)",
              "mogelijk, maar minder vanzelfsprekend"
            ],
            [
              "Multi-class",
              "OvR of multinomial/softmax",
              "meestal (OvR-achtige) strategieën"
            ],
            [
              "Wat beïnvloedt de fit?",
              "alle trainingspunten",
              "alleen de support vectors"
            ],
            [
              "Karakter",
              "kansen & confidence",
              "marges & robuust tegen overbodige data"
            ]
          ]
        }
      ]
    },
    {
      "title": "Cheat sheet: imports en hyperparameters",
      "blocks": [
        {
          "type": "table",
          "headers": [
            "Klasse",
            "Import",
            "Belangrijkste hyperparameters"
          ],
          "rows": [
            [
              "LogisticRegression",
              "<code>sklearn.linear_model</code>",
              "<code>C</code> (klein = meer regularisatie) · <code>penalty</code> (<code>l2</code>/<code>l1</code>) · multi-class-aanpak"
            ],
            [
              "LinearSVC",
              "<code>sklearn.svm</code>",
              "<code>C</code> — snelle lineaire SVM"
            ],
            [
              "SVC",
              "<code>sklearn.svm</code>",
              "<code>C</code> · <code>kernel</code> (<code>rbf</code>, <code>linear</code>, …) · <code>gamma</code> (alleen RBF: klein = glad)"
            ]
          ]
        },
        {
          "type": "callout",
          "kind": "tip",
          "html": "<p> Een lineaire SVM fitten kan ook via <code>SVC(kernel='linear')</code>, maar <code>LinearSVC</code> is vaak sneller. En er zijn méér hyperparameters dan deze — durf de documentatie te lezen. </p>"
        }
      ]
    },
    {
      "title": "SGDClassifier: het bewijs dat alleen de loss verschilt",
      "blocks": [
        {
          "type": "text",
          "html": "<code>SGDClassifier</code> kan grotere datasets aan en schakelt tussen LR en SVM via all&eacute;&eacute;n de <code>loss</code>-hyperparameter — het model is hetzelfde. Gotcha: regularisatie heet hier <code>alpha</code> (de inverse van <code>C</code>: hoger = m&eacute;&eacute;r regularisatie)."
        },
        {
          "type": "code",
          "language": "python",
          "caption": "Twee regels wisselen tussen LR en lineaire SVM",
          "runnable": true,
          "source": "from sklearn.datasets import load_wine\nfrom sklearn.linear_model import SGDClassifier\nfrom sklearn.model_selection import train_test_split\nfrom sklearn.preprocessing import StandardScaler\n\nX, y = load_wine(return_X_y=True)\nX_tr, X_te, y_tr, y_te = train_test_split(X, y, test_size=0.25, random_state=42)\nscaler = StandardScaler().fit(X_tr)\nX_tr, X_te = scaler.transform(X_tr), scaler.transform(X_te)\n\nfor loss, naam in [('hinge', 'lineaire SVM'), ('log_loss', 'logistic regression')]:\n    sgd = SGDClassifier(loss=loss, max_iter=2000, tol=1e-3, random_state=42)\n    sgd.fit(X_tr, y_tr)\n    print(f'loss={loss:<9} ({naam})  train={sgd.score(X_tr, y_tr):.3f}'\n          f'  test={sgd.score(X_te, y_te):.3f}')"
        },
        {
          "type": "callout",
          "kind": "key",
          "html": "<p> Lineaire classifiers delen één predict-stap (het teken van \\( w \\cdot x + b \\)); wat ze onderscheidt is de <strong>loss</strong> die tijdens het fitten geoptimaliseerd wordt — logistic loss, hinge loss, of iets anders. </p>"
        }
      ]
    },
    {
      "title": "Waar staat dit alles in het grotere geheel?",
      "blocks": [
        {
          "type": "text",
          "html": "Zoom uit: data science omvat alles van datacollectie tot communicatie; daarbinnen is ML een tak, supervised learning daarbinnen, classificatie daarbinnen — en daarbinnen LR en SVM's. Vervolg op deze site: tree-based methodes en calculus."
        }
      ]
    }
  ]
}
