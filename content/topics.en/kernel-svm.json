{
  "id": "kernel-svm",
  "chapter": "ch4",
  "title": "Kernel SVMs: non-linear boundaries",
  "intro": "Non-linear boundary with a linear model? Change the space in which you are linear — that is what kernels do. The RBF kernel is the standard choice.",
  "keyPoints": [
    "Non-linear data? <strong>Transform the features</strong> until they become linearly separable.",
    "The <strong>kernel trick</strong> computes transformed similarities without doing the transformation explicitly.",
    "RBF kernel: <strong>small gamma → smooth boundary</strong>; large gamma → bumpy blobs (overfitting risk).",
    "Rule of thumb: <strong>linear</strong> with many features (e.g. text), <strong>RBF</strong> with few features with structure."
  ],
  "level": 3,
  "sections": [
    {
      "title": "The transformation idea",
      "blocks": [
        {
          "type": "text",
          "html": "Take 2D data around the origin that is not separable. After the transformation \\( (x_1, x_2) \\to (x_1^2, x_2^2) \\) the classes sit bottom-left versus top-right: linearly separable, and translated back the boundary is an ellipse."
        },
        {
          "type": "formula",
          "label": "the core idea",
          "latex": "\\text{linear boundary in transformed space} \\;\\Longleftrightarrow\\; \\text{non-linear boundary in original space}",
          "caption": "The transformation need not be squaring, and the new space may even have a different number of dimensions."
        },
        {
          "type": "code",
          "language": "python",
          "caption": "Demonstrating the transformation: circle data becomes separable",
          "runnable": true,
          "source": "import numpy as np\nfrom sklearn.svm import LinearSVC, SVC\n\nrng = np.random.default_rng(42)\n\n# Red: close to the origin. Blue: in a ring around it.\nang_r = rng.uniform(0, 2 * np.pi, 50)\nr_red = rng.uniform(0, 1.0, 50)\nred = np.column_stack([r_red * np.cos(ang_r), r_red * np.sin(ang_r)])\n\nang_b = rng.uniform(0, 2 * np.pi, 80)\nr_blue = rng.uniform(2.2, 3.4, 80)\nblue = np.column_stack([r_blue * np.cos(ang_b), r_blue * np.sin(ang_b)])\n\nX = np.vstack([red, blue])\ny = np.array([0] * 50 + [1] * 80)\n\nlin = LinearSVC(max_iter=10000).fit(X, y)\nprint('LinearSVC on original X    :', round(lin.score(X, y), 3))\n\nX2 = X ** 2                      # transformation: square the features\nlin2 = LinearSVC(max_iter=10000).fit(X2, y)\nprint('LinearSVC on (x1^2, x2^2)  :', round(lin2.score(X2, y), 3))\n\nrbf = SVC().fit(X, y)\nprint('SVC with RBF kernel on X   :', round(rbf.score(X, y), 3))"
        },
        {
          "type": "callout",
          "kind": "key",
          "html": "<p> A <strong>linear model in a transformed space</strong> = a <strong>non-linear model in the original space</strong>. Kernels and kernel SVMs implement such feature transformations in a computationally efficient way. </p>"
        }
      ]
    },
    {
      "title": "The RBF kernel and the hyperparameter gamma",
      "blocks": [
        {
          "type": "text",
          "html": "For kernels use <code>SVC</code> (not <code>LinearSVC</code>); the default is the <strong>RBF kernel</strong>. Besides <code>C</code> it has its own hyperparameter <strong>gamma</strong>, which steers the <em>smoothness</em> of the boundary."
        },
        {
          "type": "formula",
          "label": "RBF kernel",
          "latex": "K(x, x') \\;=\\; e^{-\\,\\gamma\\, \\lVert x - x' \\rVert^2}",
          "caption": "small gamma = slow decay = smooth boundary · large gamma = fast decay = complex boundary."
        },
        {
          "type": "plot",
          "title": "The influence of gamma on the kernel shape",
          "subtitle": "Drag the slider: the larger gamma, the \"peakier\" the kernel — and the more complex the decision boundary.",
          "xrange": [
            -3,
            3
          ],
          "yrange": [
            -0.05,
            1.1
          ],
          "xlabel": "distance to support vector",
          "curves": [
            {
              "expr": "Math.exp(-gamma * x * x)",
              "label": "K(x, 0)",
              "color": "magenta"
            }
          ],
          "slider": {
            "param": "gamma",
            "min": 0.1,
            "max": 5,
            "step": 0.1,
            "value": 1,
            "label": "γ (gamma)"
          }
        },
        {
          "type": "text",
          "html": "<code>gamma=0.01</code> gives a smooth boundary; <code>gamma=2</code> creates &ldquo;islands&rdquo; around every training example (100% training accuracy). Large gamma + high train score = overfitting — balance with a test set."
        },
        {
          "type": "code",
          "language": "python",
          "caption": "gamma sweep: smoothness vs. complexity",
          "runnable": true,
          "source": "for gamma in [0.1, 1.0, 10.0]:\n    svc = SVC(gamma=gamma).fit(X, y)\n    print(f'gamma={gamma:<5} train={svc.score(X, y):.3f}'\n          f'  n_support={len(svc.support_vectors_)}')"
        },
        {
          "type": "callout",
          "kind": "warning",
          "html": "<p> Small gamma → simpler, smoother boundaries (less complexity). Large gamma → more complex boundaries. Together with C this determines how strong your overfitting risk is — always experiment with train <em>and</em> test! </p>"
        }
      ]
    }
  ]
}
