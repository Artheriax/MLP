{
  "id": "loss-diagrams",
  "chapter": "ch2",
  "title": "Loss diagrams: logistic loss vs. hinge loss",
  "intro": "The conceptual core of the course: loss plotted against the raw model output. Understand these diagrams and you know what logistic regression and SVMs are.",
  "keyPoints": [
    "A loss diagram plots loss against the <strong>raw model output</strong> (positive = correctly classified).",
    "Squared loss <strong>fails</strong> for classification: confident correct predictions still get penalised.",
    "<strong>Logistic loss</strong> and <strong>hinge loss</strong> are the two workhorses: smooth, optimisable and strict on mistakes.",
    "The <strong>shape</strong> of the curve drives learning: smooth = gradients, shoulder = margin."
  ],
  "level": 2,
  "sections": [
    {
      "title": "Setting up the diagram",
      "blocks": [
        {
          "type": "text",
          "html": "The axes: vertically the <strong>loss</strong>, horizontally the <strong>raw model output</strong> \\( f(x) = w \\cdot x + b \\). For one training example of class <strong>+1</strong>, the right half (positive output) is correct and the left half incorrect. This is the loss per example; total loss is the sum over all examples."
        },
        {
          "type": "plot",
          "title": "The 0-1 loss of one example (y = +1)",
          "subtitle": "Left (negative raw output): wrong → loss 1. Right: correct → loss 0.",
          "xrange": [
            -4,
            4
          ],
          "yrange": [
            -0.3,
            2
          ],
          "curves": [
            {
              "expr": "(x < 0 ? 1 : 0)",
              "label": "0-1 loss",
              "color": "yellow"
            }
          ]
        }
      ]
    },
    {
      "title": "Why the squared loss fails for classification",
      "blocks": [
        {
          "type": "text",
          "html": "Squared loss fails here: a large <em>positive</em> raw output is a correct, confident prediction — yet the quadratic penalty punishes it hard. Hence: dedicated loss functions for classification."
        },
        {
          "type": "plot",
          "title": "Squared loss vs. the raw model output (y = 1)",
          "subtitle": "The right arm rises — correct, confident predictions get punished.",
          "xrange": [
            -4,
            4
          ],
          "yrange": [
            -0.5,
            4
          ],
          "curves": [
            {
              "expr": "(x - 1) * (x - 1)",
              "label": "(x − 1)²",
              "color": "cyan"
            }
          ]
        }
      ]
    },
    {
      "title": "The two stars: logistic loss and hinge loss",
      "blocks": [
        {
          "type": "text",
          "html": "<strong>Logistic loss</strong> (logistic regression): a smooth version of 0-1 loss — the &ldquo;count the errors&rdquo; interpretation is gone, but it is minimizable. <strong>Hinge loss</strong> (SVM): same basic shape, but exactly zero from raw output <strong>+1</strong> — the flat part that creates support vectors."
        },
        {
          "type": "plot",
          "title": "All three classification losses together",
          "subtitle": "Move over the chart to read off exact values.",
          "xrange": [
            -4,
            4
          ],
          "yrange": [
            -0.4,
            5
          ],
          "curves": [
            {
              "expr": "(x < 0 ? 1 : 0)",
              "label": "0-1 loss",
              "color": "yellow"
            },
            {
              "expr": "Math.log(1 + Math.exp(-x))",
              "label": "logistic loss",
              "color": "cyan"
            },
            {
              "expr": "Math.max(0, 1 - x)",
              "label": "hinge loss",
              "color": "magenta"
            }
          ]
        },
        {
          "type": "formula",
          "label": "logistic loss",
          "latex": "L_{\\text{logistic}}\\bigl( y,\\, f(x) \\bigr) \\;=\\; \\log\\!\\left( 1 + e^{-\\,y\\, f(x)} \\right), \\qquad y \\in \\{-1,\\,+1\\}",
          "caption": "The loss of logistic regression (for one example)."
        },
        {
          "type": "formula",
          "label": "hinge loss",
          "latex": "L_{\\text{hinge}}\\bigl( y,\\, f(x) \\bigr) \\;=\\; \\max\\!\\bigl(\\, 0,\\; 1 - y\\, f(x) \\,\\bigr)",
          "caption": "The loss of SVMs — exactly 0 as soon as y·f(x) ≥ 1 (the \"flat section\")."
        },
        {
          "type": "code",
          "language": "python",
          "caption": "The losses side by side, numerically",
          "runnable": true,
          "source": "import numpy as np\n\nraw = np.array([2.0, 0.5, -0.5, -2.0])\ny = 1  # the example belongs to class +1\n\nhinge    = np.maximum(0, 1 - y * raw)\nlogistic = np.log(1 + np.exp(-y * raw))\nzero_one = (np.sign(raw) != y).astype(int)\n\nprint(f\"{'raw':>6} {'0-1':>4} {'hinge':>7} {'logistic':>9}\")\nfor r, z, h, l in zip(raw, zero_one, hinge, logistic):\n    print(f'{r:+6.1f} {z:4d} {h:7.3f} {l:9.3f}')"
        },
        {
          "type": "callout",
          "kind": "key",
          "html": "<p> These two curve shapes <strong>are</strong> the essence of logistic regression and SVMs. Everything that follows — regularisation, probabilities, support vectors, kernels — builds on these diagrams. </p>"
        }
      ]
    }
  ]
}
