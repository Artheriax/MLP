From 6b5832102d81a2b4f387ca7733faff47aec38d39 Mon Sep 17 00:00:00 2001
From: Z User <z@container>
Date: Thu, 3 Sep 2026 16:09:13 +0000
Subject: [PATCH] Dark/light-thema met pastel-accenten (donker = default, zoals
 origineel), Lexend-font, NL-default taal, uitklapbare hoofdstukken +
 inklapbare zijbalk en zoekopdracht door de volledige sub-topic-inhoud

---
 README.md     |  60 +++++---
 css/style.css | 355 ++++++++++++++++++++++++++++++++++++-------
 index.html    |  47 ++++--
 js/app.js     | 406 +++++++++++++++++++++++++++++++++++++++++++-------
 js/blocks.js  |  34 +++--
 js/content.js |  15 +-
 js/i18n.js    |  30 ++--
 js/theme.js   |  78 ++++++++++
 8 files changed, 863 insertions(+), 162 deletions(-)
 create mode 100644 js/theme.js

diff --git a/README.md b/README.md
index f403f10..fa708d9 100644
--- a/README.md
+++ b/README.md
@@ -1,9 +1,8 @@
 # MLP — Machine Learning Practice
 
 Een strakke, modulaire studiewebsite in **schone wiki-stijl**
-(geïnspireerd op GitHub wiki: licht thema, rustige typografie,
-zijbalk-navigatie — geen afleidende franje) met samenvattingen van
-de cursus
+(geïnspireerd op GitHub wiki: rustige typografie, zijbalk-navigatie
+— geen afleidende franje) met samenvattingen van de cursus
 **Logistic Regression & SVM's met scikit-learn** — van de **eerste
 basisbegrippen** (wat is ML, soorten leren, data & workflow) tot de
 classifiers — inclusief formules, interactieve diagrammen en
@@ -20,10 +19,23 @@ eigenlijk geen regressie is** ontrafelt de verwarrendste naam in ML,
 bevat de gesloten formules voor **b1 (helling) en b0 (y-snijpunt)** van
 best-fit lijn. Interactieve plots hebben **live metrics** (MSE, R²,
 accuracy, precision/recall/F1 …) die realtime meebewegen met de
-sliders en groen oplichten bij de best haalbare fit. De navigatie
-loopt via een **vaste zijbalk** (hoofdstukken + onderwerpen +
-zoekfilter); op mobiel klapt die open via het menu-icoon. De site is
-volledig **tweetalig: Nederlands en Engels** (schakelen met de
+sliders en groen oplichten bij de best haalbare fit.
+
+**Thema & typografie**: de site heeft een **dark- en een light mode**
+(schakelen met de zon/maan-knop rechtsboven, keuze wordt onthouden).
+Donker is de default — de originele stijl: achtergrond `#161616` met
+**pastel-accenten** (cyan, geel, light green, magenta). In de lichte
+modus is de achtergrond wit met zwarte tekst en iets verdiepte
+pastel-tinten voor leesbaarheid. Het lettertype is **Lexend**
+(oogvriendelijk, via Google Fonts met nette systeem-fallback). De
+**navigatie** loopt via een vaste **inklapbare zijbalk** met
+**uitklapbare hoofdstukken** (klik op een hoofdstuk om de sub-topics
+te zien; de stand wordt onthouden en het hoofdstuk van het actieve
+onderwerp klapt vanzelf open). De **zoekbalk doorzoekt de volledige
+inhoud** van alle sub-topics — teksten, formules, code en tabellen —
+en toont resultaten met een gemarkeerd snippet (sneltoets: `/`);
+op mobiel klapt de zijbalk open via het menu-icoon. De site is
+volledig **tweetalig: Nederlands (default) en Engels** (schakelen met
 NL/EN-knop rechtsboven).
 
 - **Frontend**: pure HTML/CSS/JS — geen build-stap, geen framework
@@ -118,10 +130,22 @@ lesstof toe te voegen:
 
 - De taal staat in `js/i18n.js` (UI-strings) + de content-mappen:
   `content/topics/` (NL) en `content/topics.en/` (EN).
-- De keuze wordt onthouden in `localStorage` (sleutel `mlp-lang`); zonder
-  voorkeur kiest de site op basis van de browsertaal.
+- De keuze wordt onthouden in `localStorage` (sleutel `mlp-lang`);
+  **zonder opgeslagen voorkeur is Nederlands de default** — de
+  browsertaal telt niet (EN is de secundaire optie via de schakelaar).
 - In de API-modus vraag je de Engelse versie op met `?lang=en`.
 
+### Thema (dark/light)
+
+- De schakelaar staat rechtsboven (zon/maan-icoon); donker is de
+  default (`#161616` + pastel-accenten).
+- De keuze wordt onthouden in `localStorage` (sleutel `mlp-theme`).
+- Alle kleuren — ook die van plots, formule-accents en
+  syntax-highlighting — lopen via CSS-variabelen en wisselen dus
+  automatisch mee.
+- Zijbalk-standen: `mlp-sidebar-collapsed` (hele zijbalk) en
+  `mlp-nav-open` (welke hoofdstukken open staan).
+
 ### Schema van een onderwerp
 
 ```jsonc
@@ -194,14 +218,15 @@ waarheid. Zonder `lang`-parameter krijg je de NL-versie.
 
 ```
 MLP/
-├── index.html            # enige HTML-pagina (SPA met hash-routering #/onderwerp/<id> + NL/EN-schakelaar)
-├── css/style.css         # volledig ontwerp (licht wiki-thema · GitHub-achtig · systeemfonts)
+├── index.html            # enige HTML-pagina (SPA met hash-routering #/onderwerp/<id> + NL/EN + theme-schakelaar)
+├── css/style.css         # volledig ontwerp (dark + light · pastel-accenten · Lexend · inklapbare zijbalk)
 ├── js/
-│   ├── i18n.js           # tweetaligheid: alle UI-strings (NL/EN), taalkeuze + -opslag
+│   ├── i18n.js           # tweetaligheid: alle UI-strings (NL/EN), NL-default, taalkeuze + -opslag
+│   ├── theme.js          # dark/light-schakelaar (localStorage + thema-knop)
 │   ├── content.js        # datalaag: FastAPI-API (?lang=) of statische JSON (NL/EN + fallback)
-│   ├── blocks.js         # renderers: tekst, formules (KaTeX), code-velden (uitvoeren + kopiëren), callouts, tabellen, plots (punten + sliders + markers + live metrics)
+│   ├── blocks.js         # renderers: tekst, formules (KaTeX), code-velden (uitvoeren + kopiëren), callouts, tabellen, plots (punten + sliders + markers + live metrics, thema-onafhankelijk)
 │   ├── runner.js         # Pyodide-runner: code in de browser uitvoeren (lazy geladen)
-│   └── app.js            # router, zijbalk-navigatie (hoofdstukken + zoekfilter), startpagina, taalwissel
+│   └── app.js            # router, zijbalk (uitklapbare hoofdstukken + inklapbaar), inhoud-zoekopdracht, startpagina, taalwissel
 ├── content/
 │   ├── index.json        # NL: hoofdstukken + onderwerpen-register
 │   ├── index.en.json     # EN: idem
@@ -216,9 +241,10 @@ MLP/
 ```
 
 **Externe libraries (via CDN, dus internet nodig):** KaTeX (formules),
-highlight.js (syntax-kleuring) en Pyodide (code-uitvoering). Typografie
-gebruikt de systeemfonts van het besturingssysteem (geen webfonts).
-Zonder internet werkt de site nog steeds — formules/code tonen dan platte tekst.
+highlight.js (syntax-kleuring), Pyodide (code-uitvoering) en Google
+Fonts (**Lexend**). Zonder internet werkt de site nog steeds — de
+typografie valt dan netjes terug op systeemfonts en formules/code
+tonen platte tekst.
 
 **Waarom geen framework?** De opdracht vraagt om een HTML-site die op GitHub
 Pages moet draaien (statisch). Met een pure frontend is er geen build-stap,
diff --git a/css/style.css b/css/style.css
index b96dc16..fc92e69 100644
--- a/css/style.css
+++ b/css/style.css
@@ -1,10 +1,23 @@
 /* ============================================================
    MLP — Machine Learning Practice
-   Design: clean, minimaal wiki-thema (geïnspireerd op GitHub wiki)
-   — licht thema · systeemfonts · rustige kleuren · geen franje
+   Design: clean, minimaal wiki-thema (GitHub-wiki-achtig)
+   · Lexend (oogvriendelijk) · inklapbare zijbalk met uitklapbare
+     hoofdstukken · dark + light thema met pastel-accenten
+   · Dark (default, zoals het origineel): #161616 + pastel
+     cyan/geel/groen/magenta · Light: wit + zwarte tekst
    ============================================================ */
 
 :root {
+  --sidebar-w: 272px;
+  --content-w: 840px;
+  --topbar-h: 48px;
+}
+
+/* ---------- Licht thema (wit · zwart · pastel-tinten) ---------- */
+
+:root, [data-theme="light"] {
+  color-scheme: light;
+
   --bg: #ffffff;
   --bg-subtle: #f6f8fa;
   --bg-inset: #eef1f4;
@@ -12,26 +25,71 @@
   --border: #d1d9e0;
   --border-muted: #e4e8eb;
 
-  --text: #1f2328;
+  --text: #161616;
   --text-muted: #59636e;
   --text-faint: #818b98;
 
-  --link: #0969da;
-  --link-hover: #0550ae;
+  /* pastel-accenten, iets verdiept voor leesbaarheid op wit */
+  --cyan: #1296b0;
+  --yellow: #a07d1f;
+  --green: #2f9d55;
+  --magenta: #c14f9a;
+
+  --link: #1296b0;
+  --link-hover: #0c788c;
+  --danger: #cf222e;
+
+  --plot-bg: #ffffff;
+  --plot-grid: rgba(31, 35, 40, 0.08);
+  --plot-axis: rgba(31, 35, 40, 0.45);
+  --plot-tick: rgba(31, 35, 40, 0.65);
+  --plot-cross: rgba(31, 35, 40, 0.30);
+  --plot-point-ring: #ffffff;
+  --plot-tooltip-bg: rgba(255, 255, 255, 0.97);
 
-  /* hoofdstuk-accenten (namen blijven gelijk aan de content,
-     waarden zijn aangepast aan het lichte thema) */
-  --cyan: #0969da;    /* blauw */
-  --yellow: #9a6700;  /* amber */
-  --green: #1a7f37;
-  --magenta: #8250df; /* paars */
+  --shadow: 0 2px 10px rgba(31, 35, 40, 0.12);
+  --shadow-side: 8px 0 24px rgba(31, 35, 40, 0.08);
 
-  --font-body: -apple-system, BlinkMacSystemFont, "Segoe UI", "Noto Sans", Helvetica, Arial, sans-serif;
+  --font-body: "Lexend", -apple-system, BlinkMacSystemFont, "Segoe UI", "Noto Sans", Helvetica, Arial, sans-serif;
   --font-mono: ui-monospace, SFMono-Regular, "SF Mono", Menlo, Consolas, "Liberation Mono", monospace;
+}
 
-  --sidebar-w: 268px;
-  --content-w: 840px;
-  --topbar-h: 48px;
+/* ---------- Donker thema (default · #161616 · originele pastel) ---------- */
+
+[data-theme="dark"] {
+  color-scheme: dark;
+
+  --bg: #161616;
+  --bg-subtle: #1b1b1b;
+  --bg-inset: #1e1e1e;
+
+  --border: rgba(255, 255, 255, 0.10);
+  --border-muted: rgba(255, 255, 255, 0.06);
+
+  --text: #ffffff;
+  --text-muted: rgba(255, 255, 255, 0.66);
+  --text-faint: rgba(255, 255, 255, 0.42);
+
+  /* de pastel-kleuren zoals in de eerste versie van de site */
+  --cyan: #7FDBDA;
+  --yellow: #F9E79F;
+  --green: #B9F6CA;
+  --magenta: #F6A5C0;
+
+  --link: #7FDBDA;
+  --link-hover: #a3e9e8;
+  --danger: #ff8f93;
+
+  --plot-bg: #191919;
+  --plot-grid: rgba(255, 255, 255, 0.07);
+  --plot-axis: rgba(255, 255, 255, 0.42);
+  --plot-tick: rgba(255, 255, 255, 0.62);
+  --plot-cross: rgba(255, 255, 255, 0.28);
+  --plot-point-ring: #161616;
+  --plot-tooltip-bg: rgba(27, 27, 27, 0.97);
+
+  --shadow: 0 2px 12px rgba(0, 0, 0, 0.45);
+  --shadow-side: 8px 0 24px rgba(0, 0, 0, 0.45);
 }
 
 /* ---------- Reset & basis ---------- */
@@ -58,7 +116,14 @@ body {
 
 main { flex: 1 0 auto; outline: none; }
 
-::selection { background: rgba(9, 105, 218, 0.15); }
+/* zachte overgang bij thema- of zijbalk-wissel */
+body, .topbar, .sidebar, .content, .site-footer,
+.formula-block, .callout, .table-wrap, .plot-card, .code-field,
+.data-table th, .plot-metric, .code-head, .code-output, .error-card, .noscript-card {
+  transition: background-color .18s ease, border-color .18s ease, color .18s ease;
+}
+
+::selection { background: color-mix(in srgb, var(--cyan) 24%, transparent); }
 
 a { color: var(--link); text-decoration: none; }
 a:hover { color: var(--link-hover); text-decoration: underline; }
@@ -97,8 +162,8 @@ a:hover { color: var(--link-hover); text-decoration: underline; }
   padding: 0 1rem;
 }
 
-.menu-toggle {
-  display: none;
+.menu-toggle, .icon-btn {
+  display: inline-flex;
   align-items: center;
   justify-content: center;
   width: 32px; height: 32px;
@@ -107,8 +172,9 @@ a:hover { color: var(--link-hover); text-decoration: underline; }
   background: var(--bg);
   color: var(--text-muted);
   cursor: pointer;
+  flex: 0 0 auto;
 }
-.menu-toggle:hover { background: var(--bg-subtle); color: var(--text); }
+.menu-toggle:hover, .icon-btn:hover { background: var(--bg-subtle); color: var(--text); }
 
 .brand {
   display: flex;
@@ -130,7 +196,12 @@ a:hover { color: var(--link-hover); text-decoration: underline; }
   letter-spacing: 0.06em;
 }
 
-.topbar-actions { margin-left: auto; }
+.topbar-actions {
+  margin-left: auto;
+  display: flex;
+  align-items: center;
+  gap: 0.5rem;
+}
 
 .lang-toggle {
   font: 600 0.75rem/1 var(--font-mono);
@@ -144,21 +215,41 @@ a:hover { color: var(--link-hover); text-decoration: underline; }
 }
 .lang-toggle:hover { color: var(--text); background: var(--bg-subtle); }
 
+/* thema-knop: laat zien waar je naartoe schakelt */
+#themeToggle .icon-sun { display: none; }
+#themeToggle .icon-moon { display: block; }
+[data-theme="dark"] #themeToggle .icon-sun { display: block; }
+[data-theme="dark"] #themeToggle .icon-moon { display: none; }
+
 /* ---------- Lay-out: zijbalk + inhoud ---------- */
 
 .layout {
   display: grid;
-  grid-template-columns: var(--sidebar-w) minmax(0, 1fr);
+  grid-template-columns: auto minmax(0, 1fr);
 }
 
 .sidebar {
   position: sticky;
   top: var(--topbar-h);
   height: calc(100vh - var(--topbar-h));
-  overflow-y: auto;
-  overscroll-behavior: contain;
+  width: var(--sidebar-w);
+  overflow: hidden;
   background: var(--bg-subtle);
   border-right: 1px solid var(--border);
+  transition: width .2s ease, background-color .18s ease, border-color .18s ease;
+}
+
+/* zijbalk ingeklapt (desktop) */
+body.sb-collapsed .sidebar {
+  width: 0;
+  border-right-color: transparent;
+}
+
+.sidebar-scroll {
+  height: 100%;
+  width: var(--sidebar-w);
+  overflow-y: auto;
+  overscroll-behavior: contain;
 }
 
 .sidebar-search {
@@ -176,7 +267,7 @@ a:hover { color: var(--link-hover); text-decoration: underline; }
 
 #sidebarSearch {
   width: 100%;
-  padding: 5px 8px 5px 30px;
+  padding: 5px 30px 5px 30px;
   font: 400 0.84rem/1.4 var(--font-body);
   color: var(--text);
   background: var(--bg);
@@ -186,10 +277,80 @@ a:hover { color: var(--link-hover); text-decoration: underline; }
 #sidebarSearch::placeholder { color: var(--text-faint); }
 #sidebarSearch:focus {
   outline: none;
-  border-color: var(--link);
-  box-shadow: 0 0 0 3px rgba(9, 105, 218, 0.12);
+  border-color: var(--cyan);
+  box-shadow: 0 0 0 3px color-mix(in srgb, var(--cyan) 16%, transparent);
+}
+#sidebarSearch::-webkit-search-cancel-button { cursor: pointer; }
+
+.search-kbd {
+  position: absolute;
+  right: 22px;
+  top: 19px;
+  font: 500 0.66rem/1 var(--font-mono);
+  color: var(--text-faint);
+  border: 1px solid var(--border);
+  border-radius: 4px;
+  padding: 2px 5px;
+  pointer-events: none;
+}
+
+/* ---------- Zoekresultaten (doorzoekt ook de inhoud) ---------- */
+
+.search-status {
+  padding: 2px 14px 6px;
+  font-size: 0.72rem;
+  color: var(--text-faint);
+}
+
+.search-results { display: none; }
+.sidebar.searching .sidebar-nav { display: none; }
+.sidebar.searching .search-results { display: block; padding: 2px 10px 26px; }
+
+.sr-item {
+  display: block;
+  padding: 7px 8px;
+  border-radius: 6px;
+}
+.sr-item:hover { background: var(--bg-inset); text-decoration: none; }
+
+.sr-title {
+  display: block;
+  font-size: 0.84rem;
+  font-weight: 500;
+  color: var(--text);
+}
+.sr-item:hover .sr-title { color: var(--link); }
+
+.sr-chapter {
+  display: block;
+  font-size: 0.66rem;
+  letter-spacing: 0.04em;
+  text-transform: uppercase;
+  color: var(--text-faint);
+  margin: 1px 0 3px;
+}
+
+.sr-snippet {
+  display: block;
+  font-size: 0.75rem;
+  line-height: 1.45;
+  color: var(--text-muted);
+}
+.sr-snippet mark {
+  background: color-mix(in srgb, var(--yellow) 34%, transparent);
+  color: inherit;
+  border-radius: 2px;
+  padding: 0 1px;
+}
+
+.sr-empty {
+  padding: 8px;
+  font-size: 0.8rem;
+  color: var(--text-faint);
 }
 
+/* ---------- Navigatie: uitklapbare hoofdstukken ---------- */
+
 .sidebar-nav { padding: 6px 10px 28px; }
 
 .nav-home {
@@ -202,28 +363,76 @@ a:hover { color: var(--link-hover); text-decoration: underline; }
   color: var(--text);
 }
 .nav-home:hover { background: var(--bg-inset); color: var(--text); text-decoration: none; }
-.nav-home.active { background: #ddf0ff; color: var(--text); }
+.nav-home.active { background: color-mix(in srgb, var(--cyan) 16%, var(--bg-subtle)); color: var(--text); }
 
-.nav-section { margin-top: 14px; }
+.nav-section { margin-top: 10px; }
 
 .nav-chapter {
-  padding: 2px 8px 3px;
-  font-size: 0.68rem;
-  font-weight: 600;
+  display: flex;
+  align-items: center;
+  gap: 6px;
+  width: 100%;
+  padding: 4px 8px;
+  border: none;
+  border-radius: 6px;
+  background: none;
+  cursor: pointer;
+  text-align: left;
+  font: 600 0.7rem/1.35 var(--font-body);
   letter-spacing: 0.07em;
   text-transform: uppercase;
   color: var(--text-muted);
 }
+.nav-chapter:hover { color: var(--text); background: var(--bg-inset); }
+
+.nav-chapter .chev {
+  flex: 0 0 auto;
+  color: var(--text-faint);
+  transition: transform .16s ease;
+}
+.nav-section.collapsed .nav-chapter .chev { transform: rotate(-90deg); }
+
+.nav-chapter .nav-chapter-name {
+  flex: 1 1 auto;
+  min-width: 0;
+  overflow: hidden;
+  text-overflow: ellipsis;
+  white-space: nowrap;
+}
+
+.nav-count {
+  flex: 0 0 auto;
+  font: 500 0.64rem/1 var(--font-mono);
+  color: var(--text-faint);
+}
+
+/* vloeiend in-/uitklappen van de onderwerpenlijst */
+.nav-list-wrap {
+  display: grid;
+  grid-template-rows: 1fr;
+  transition: grid-template-rows .18s ease;
+}
+.nav-section.collapsed .nav-list-wrap { grid-template-rows: 0fr; }
+
+.nav-list {
+  overflow: hidden;
+  min-height: 0;
+  padding-top: 1px;
+}
 
 .nav-link {
   display: block;
-  padding: 4px 8px;
+  padding: 4px 8px 4px 20px;
   border-radius: 6px;
   font-size: 0.84rem;
   color: var(--text);
 }
 .nav-link:hover { background: var(--bg-inset); color: var(--text); text-decoration: none; }
-.nav-link.active { background: #ddf0ff; color: var(--text); font-weight: 600; }
+.nav-link.active {
+  background: color-mix(in srgb, var(--cyan) 16%, var(--bg-subtle));
+  color: var(--text);
+  font-weight: 600;
+}
 
 .nav-empty {
   padding: 6px 8px;
@@ -361,7 +570,7 @@ a:hover { color: var(--link-hover); text-decoration: underline; }
 .formula-block {
   background: var(--bg-subtle);
   border: 1px solid var(--border);
-  border-left: 3px solid var(--accent, var(--link));
+  border-left: 3px solid var(--accent, var(--cyan));
   border-radius: 6px;
   padding: 0.95rem 1.25rem 0.8rem;
   overflow-x: auto;
@@ -373,7 +582,7 @@ a:hover { color: var(--link-hover); text-decoration: underline; }
   font-weight: 500;
   letter-spacing: 0.12em;
   text-transform: uppercase;
-  color: var(--text-muted);
+  color: var(--accent, var(--cyan));
   margin-bottom: 0.35rem;
 }
 
@@ -391,8 +600,8 @@ a:hover { color: var(--link-hover); text-decoration: underline; }
   display: flex;
   gap: 0.85rem;
   align-items: flex-start;
-  border-left: 3px solid var(--accent, var(--link));
-  background: color-mix(in srgb, var(--accent, #0969da) 5%, #ffffff);
+  border-left: 3px solid var(--accent, var(--cyan));
+  background: color-mix(in srgb, var(--accent, var(--cyan)) 6%, var(--bg));
   border-radius: 0 6px 6px 0;
   padding: 0.8rem 1.1rem;
 }
@@ -404,7 +613,7 @@ a:hover { color: var(--link-hover); text-decoration: underline; }
   font-weight: 700;
   letter-spacing: 0.12em;
   text-transform: uppercase;
-  color: var(--accent, var(--link));
+  color: var(--accent, var(--cyan));
   margin-top: 0.2rem;
 }
 
@@ -474,17 +683,24 @@ a:hover { color: var(--link-hover); text-decoration: underline; }
   width: 100%;
   height: auto;
   border-radius: 6px;
-  background: #ffffff;
+  background: var(--plot-bg);
   border: 1px solid var(--border-muted);
 }
 
+/* SVG-elementen volgen het thema via CSS */
+.plot-grid line { stroke: var(--plot-grid); }
+.plot-grid { stroke-width: 1; }
+svg .plot-axis { stroke: var(--plot-axis); stroke-width: 1.2; }
+svg .plot-tick { fill: var(--plot-tick); }
+svg .plot-cross { stroke: var(--plot-cross); stroke-dasharray: 4 4; stroke-width: 1; }
+
 .plot-tooltip {
   position: absolute;
   pointer-events: none;
-  background: rgba(255, 255, 255, 0.97);
+  background: var(--plot-tooltip-bg);
   border: 1px solid var(--border);
   border-radius: 6px;
-  box-shadow: 0 2px 8px rgba(31, 35, 40, 0.12);
+  box-shadow: var(--shadow);
   padding: 0.4rem 0.65rem;
   font-family: var(--font-mono);
   font-size: 0.72rem;
@@ -527,13 +743,13 @@ a:hover { color: var(--link-hover); text-decoration: underline; }
 
 .plot-slider input[type="range"] {
   flex: 1 1 150px;
-  accent-color: var(--accent, var(--link));
+  accent-color: var(--accent, var(--cyan));
   cursor: pointer;
 }
 
 .plot-slider .slider-val {
   font-family: var(--font-mono);
-  color: var(--accent, var(--link));
+  color: var(--accent, var(--cyan));
   min-width: 3.2em;
   text-align: right;
 }
@@ -562,7 +778,7 @@ a:hover { color: var(--link-hover); text-decoration: underline; }
   width: 6px;
   height: 6px;
   border-radius: 50%;
-  background: var(--accent, var(--link));
+  background: var(--accent, var(--cyan));
   opacity: 0.6;
   flex: 0 0 auto;
 }
@@ -583,7 +799,7 @@ a:hover { color: var(--link-hover); text-decoration: underline; }
 
 .plot-metric.good {
   border-color: color-mix(in srgb, var(--green) 45%, transparent);
-  background: color-mix(in srgb, var(--green) 8%, #ffffff);
+  background: color-mix(in srgb, var(--green) 9%, var(--bg));
 }
 
 .plot-metric.good .pm-dot { background: var(--green); opacity: 1; }
@@ -693,7 +909,7 @@ a:hover { color: var(--link-hover); text-decoration: underline; }
 .output-label .spinner {
   width: 11px; height: 11px;
   border: 2px solid var(--border);
-  border-top-color: var(--link);
+  border-top-color: var(--cyan);
   border-radius: 50%;
   animation: spin .8s linear infinite;
   display: none;
@@ -715,7 +931,7 @@ a:hover { color: var(--link-hover); text-decoration: underline; }
   overflow-y: auto;
 }
 
-.code-output pre.is-error { color: #cf222e; }
+.code-output pre.is-error { color: var(--danger); }
 
 .output-hint {
   font-size: 0.75rem;
@@ -771,7 +987,7 @@ a:hover { color: var(--link-hover); text-decoration: underline; }
   max-width: 560px;
   background: var(--bg-subtle);
   border: 1px solid var(--border);
-  border-left: 3px solid #cf222e;
+  border-left: 3px solid var(--danger);
   border-radius: 6px;
   padding: 1.4rem 1.6rem;
 }
@@ -804,8 +1020,9 @@ a:hover { color: var(--link-hover); text-decoration: underline; }
   font-size: 0.78rem;
 }
 
-/* ---------- Syntax highlighting (licht, GitHub-achtig) ---------- */
+/* ---------- Syntax highlighting ---------- */
 
+/* licht: GitHub-achtig, rustig */
 .hljs-comment, .hljs-quote { color: #6e7781; font-style: italic; }
 .hljs-keyword, .hljs-selector-tag, .hljs-literal { color: #cf222e; }
 .hljs-string { color: #0a3069; }
@@ -817,13 +1034,39 @@ a:hover { color: var(--link-hover); text-decoration: underline; }
 .hljs-meta, .hljs-doctag { color: #6e7781; }
 .hljs-operator, .hljs-punctuation { color: var(--text-muted); }
 
+/* donker: de pastel-accenten van het thema */
+[data-theme="dark"] .hljs-comment,
+[data-theme="dark"] .hljs-quote,
+[data-theme="dark"] .hljs-meta,
+[data-theme="dark"] .hljs-doctag { color: rgba(255, 255, 255, 0.40); }
+
+[data-theme="dark"] .hljs-keyword,
+[data-theme="dark"] .hljs-selector-tag,
+[data-theme="dark"] .hljs-literal { color: #F6A5C0; }
+
+[data-theme="dark"] .hljs-string { color: #B9F6CA; }
+
+[data-theme="dark"] .hljs-number,
+[data-theme="dark"] .hljs-symbol { color: #F9E79F; }
+
+[data-theme="dark"] .hljs-title,
+[data-theme="dark"] .hljs-title.function_,
+[data-theme="dark"] .hljs-built_in,
+[data-theme="dark"] .hljs-name,
+[data-theme="dark"] .hljs-attr { color: #7FDBDA; }
+
+[data-theme="dark"] .hljs-title.class_,
+[data-theme="dark"] .hljs-type { color: #F9E79F; }
+
+[data-theme="dark"] .hljs-params { color: var(--text); }
+[data-theme="dark"] .hljs-operator,
+[data-theme="dark"] .hljs-punctuation { color: rgba(255, 255, 255, 0.66); }
+
 /* ---------- Responsive ---------- */
 
 @media (max-width: 920px) {
   .layout { display: block; }
 
-  .menu-toggle { display: inline-flex; }
-
   .sidebar {
     position: fixed;
     top: var(--topbar-h);
@@ -834,14 +1077,18 @@ a:hover { color: var(--link-hover); text-decoration: underline; }
     transform: translateX(-100%);
     transition: transform .22s ease;
     z-index: 25;
-    box-shadow: 8px 0 24px rgba(31, 35, 40, 0.08);
+    box-shadow: var(--shadow-side);
   }
   .sidebar.open { transform: translateX(0); }
 
+  body.sb-collapsed .sidebar { width: min(82vw, 300px); }
+
+  .sidebar-scroll { width: 100%; }
+
   .sidebar-backdrop {
     position: fixed;
     inset: var(--topbar-h) 0 0 0;
-    background: rgba(31, 35, 40, 0.35);
+    background: rgba(0, 0, 0, 0.42);
     z-index: 20;
   }
 
@@ -850,6 +1097,8 @@ a:hover { color: var(--link-hover); text-decoration: underline; }
   .content { padding: 20px 18px 72px; }
 
   .home-list { grid-template-columns: 1fr; }
+
+  .search-kbd { display: none; }
 }
 
 @media (max-width: 540px) {
diff --git a/index.html b/index.html
index cb000b9..bc83b5f 100644
--- a/index.html
+++ b/index.html
@@ -5,8 +5,27 @@
   <meta name="viewport" content="width=device-width, initial-scale=1.0">
   <title>MLP — Machine Learning samengevat</title>
   <meta name="description" content="Machine Learning stof helder samengevat: van de basis tot scikit-learn, loss functions, logistic regression en SVM's — met formules, voorbeelden en uitvoerbare code.">
-  <meta name="color-scheme" content="light">
-  <link rel="icon" href="data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 64 64'%3E%3Crect width='64' height='64' rx='10' fill='%23ffffff' stroke='%23d1d9e0'/%3E%3Ctext x='32' y='41' font-family='ui-monospace, Menlo, monospace' font-size='22' font-weight='700' fill='%230969da' text-anchor='middle'%3EML%3C/text%3E%3C/svg%3E">
+  <meta name="color-scheme" content="dark light">
+  <meta name="theme-color" content="#161616">
+
+  <!-- Thema vóór het eerste paint zetten (voorkomt kleurflits) -->
+  <script>
+    (function () {
+      var theme = "dark"; /* zoals het origineel: donker + pastel */
+      try {
+        var stored = localStorage.getItem("mlp-theme");
+        if (stored === "light" || stored === "dark") theme = stored;
+      } catch (e) { /* localStorage geblokkeerd → donker */ }
+      document.documentElement.setAttribute("data-theme", theme);
+    })();
+  </script>
+
+  <link rel="icon" href="data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 64 64'%3E%3Crect width='64' height='64' rx='12' fill='%23161616' stroke='%23303030'/%3E%3Ctext x='32' y='42' font-family='Lexend, Arial, sans-serif' font-size='23' font-weight='700' fill='%237FDBDA' text-anchor='middle'%3EML%3C/text%3E%3C/svg%3E">
+
+  <!-- Lexend: rustig, oogvriendelijk font -->
+  <link rel="preconnect" href="https://fonts.googleapis.com">
+  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
+  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Lexend:wght@300..700&display=swap">
 
   <!-- KaTeX voor formules -->
   <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/katex@0.16.11/dist/katex.min.css" crossorigin="anonymous">
@@ -25,22 +44,31 @@
         <span class="brand-sub">machine&nbsp;learning&nbsp;practice</span>
       </a>
       <div class="topbar-actions">
+        <button type="button" class="icon-btn" id="themeToggle" aria-label="Schakel naar licht thema" title="Schakel naar licht thema">
+          <svg class="icon-moon" viewBox="0 0 16 16" width="15" height="15" fill="none" stroke="currentColor" stroke-width="1.6" aria-hidden="true"><path d="M13.8 10A6.1 6.1 0 0 1 6 2.2 6.1 6.1 0 1 0 13.8 10z"/></svg>
+          <svg class="icon-sun" viewBox="0 0 16 16" width="15" height="15" fill="none" stroke="currentColor" stroke-width="1.6" aria-hidden="true"><circle cx="8" cy="8" r="3.1"/><path d="M8 1.3v1.7M8 13v1.7M1.3 8H3M13 8h1.7M3.3 3.3l1.2 1.2M11.5 11.5l1.2 1.2M12.7 3.3l-1.2 1.2M4.5 11.5l-1.2 1.2"/></svg>
+        </button>
         <button type="button" class="lang-toggle" id="langToggle" aria-label="Switch to English" title="Switch to English">EN</button>
       </div>
     </div>
   </header>
 
-  <div class="layout">
+  <div class="layout" id="layout">
     <div class="sidebar-backdrop" id="sidebarBackdrop" hidden></div>
 
     <aside class="sidebar" id="sidebar">
-      <div class="sidebar-search">
-        <svg viewBox="0 0 16 16" width="14" height="14" fill="none" stroke="currentColor" stroke-width="1.6" aria-hidden="true"><circle cx="7" cy="7" r="4.5"/><path d="M10.5 10.5L14 14"/></svg>
-        <input type="search" id="sidebarSearch" placeholder="Zoeken…" aria-label="Zoek in onderwerpen" autocomplete="off">
+      <div class="sidebar-scroll">
+        <div class="sidebar-search">
+          <svg viewBox="0 0 16 16" width="14" height="14" fill="none" stroke="currentColor" stroke-width="1.6" aria-hidden="true"><circle cx="7" cy="7" r="4.5"/><path d="M10.5 10.5L14 14"/></svg>
+          <input type="search" id="sidebarSearch" placeholder="Zoek in inhoud…" aria-label="Zoek in onderwerpen en inhoud" autocomplete="off">
+          <kbd class="search-kbd" aria-hidden="true">/</kbd>
+        </div>
+        <div class="search-status" id="searchStatus" hidden></div>
+        <div class="search-results" id="searchResults" aria-live="polite"></div>
+        <nav class="sidebar-nav" id="sidebarNav" aria-label="Onderwerpen">
+          <!--wordt gevuld door js/app.js-->
+        </nav>
       </div>
-      <nav class="sidebar-nav" id="sidebarNav" aria-label="Onderwerpen">
-        <!--wordt gevuld door js/app.js-->
-      </nav>
     </aside>
 
     <main class="content" id="app" tabindex="-1">
@@ -65,6 +93,7 @@
 
   <!-- App -->
   <script defer src="js/i18n.js"></script>
+  <script defer src="js/theme.js"></script>
   <script defer src="js/content.js"></script>
   <script defer src="js/blocks.js"></script>
   <script defer src="js/runner.js"></script>
diff --git a/js/app.js b/js/app.js
index 52148e4..ec4b534 100644
--- a/js/app.js
+++ b/js/app.js
@@ -1,8 +1,11 @@
 /* ============================================================
    MLP — app.js
-   Hash-router + views: sidebar-navigatie (hoofdstukken +
-   onderwerpen met zoekfilter), wiki-achtige startpagina en
-   onderwerppagina. Koppelt alles aan elkaar.
+   Hash-router + views. Nieuw in deze versie:
+   - zijbalk met UITKLAPBARE hoofdstukken (topics → sub-topics),
+     inclusief herinnerde stand en auto-open van het actieve hoofdstuk
+   - de hele zijbalk is inklapbaar (desktop + mobiele drawer)
+   - de zoekbalk doorzoekt de volledige inhoud van alle sub-topics
+     (tekst, formules, code, tabellen) met snippets + markeerlicht
    Alle zichtbare teksten komen uit js/i18n.js (NL/EN).
    ============================================================ */
 
@@ -54,10 +57,47 @@ window.MLP = window.MLP || {};
   }
 
   /* ============================================================
-     Sidebar — hoofdstukken + onderwerpen, met zoekfilter
+     Sidebar — hoofdstukken (uitklapbaar) + onderwerpen
      ============================================================ */
 
   let sidebarBuilt = false;
+  const NAV_KEY = "mlp-nav-open";
+  const SB_KEY = "mlp-sidebar-collapsed";
+
+  function loadNavOpen() {
+    try {
+      const raw = localStorage.getItem(NAV_KEY);
+      if (raw) return JSON.parse(raw);
+    } catch (e) {
+      /* corrupte waarde negeren */
+    }
+    return null; /* null = eerste bezoek: alleen het eerste hoofdstuk open */
+  }
+
+  function saveNavOpen(ids) {
+    try {
+      localStorage.setItem(NAV_KEY, JSON.stringify(ids));
+    } catch (e) {
+      /* voorkeur onthouden is optioneel */
+    }
+  }
+
+  function navOpenIds() {
+    return $$(".nav-section")
+      .filter((s) => !s.classList.contains("collapsed"))
+      .map((s) => s.dataset.chapter);
+  }
+
+  function setChapterOpen(sec, open, persist) {
+    sec.classList.toggle("collapsed", !open);
+    const btn = $(".nav-chapter", sec);
+    if (btn) btn.setAttribute("aria-expanded", open ? "true" : "false");
+    if (persist) saveNavOpen(navOpenIds());
+  }
+
+  function toggleChapter(sec) {
+    setChapterOpen(sec, sec.classList.contains("collapsed"), true);
+  }
 
   async function ensureSidebar() {
     if (sidebarBuilt) return;
@@ -70,6 +110,8 @@ window.MLP = window.MLP || {};
     buildSidebar(index);
     sidebarBuilt = true;
     applySidebarFilter();
+    /* zoekindex (volledige inhoud) op de achtergrond bouwen */
+    buildSearchIndex();
   }
 
   function buildSidebar(index) {
@@ -87,14 +129,29 @@ window.MLP = window.MLP || {};
       if (ch) tp._chapterName = ch.name;
     });
 
-    chapters.forEach((ch) => {
+    const storedOpen = loadNavOpen();
+
+    chapters.forEach((ch, ci) => {
       const topics = (index.topics || []).filter((tp) => tp.chapter === ch.id);
       if (!topics.length) return;
 
       const sec = el("div", "nav-section");
       sec.dataset.chapter = ch.id;
-      sec.appendChild(el("div", "nav-chapter", escapeHtml(ch.name)));
 
+      /* hoofdstuk-knop: klapt de sub-topics open/dicht */
+      const head = el("button", "nav-chapter");
+      head.type = "button";
+      head.setAttribute("aria-expanded", "true");
+      head.setAttribute("aria-controls", "navlist-" + ch.id);
+      head.innerHTML =
+        '<svg class="chev" viewBox="0 0 16 16" width="12" height="12" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true"><path d="M5.5 4L10 8l-4.5 4"/></svg>' +
+        '<span class="nav-chapter-name">' + escapeHtml(ch.name) + "</span>" +
+        '<span class="nav-count">' + topics.length + "</span>";
+      head.addEventListener("click", () => toggleChapter(sec));
+      sec.appendChild(head);
+
+      const wrap = el("div", "nav-list-wrap");
+      wrap.id = "navlist-" + ch.id;
       const list = el("div", "nav-list");
       topics.forEach((tp) => {
         const a = el("a", "nav-link", escapeHtml(tp.title));
@@ -105,46 +162,16 @@ window.MLP = window.MLP || {};
         ).toLowerCase();
         list.appendChild(a);
       });
-      sec.appendChild(list);
-      nav.appendChild(sec);
-    });
-  }
-
-  /* zoekfilter in de sidebar: verbergt niet-passende onderwerpen
-     en hoofdstukken zonder zichtbare onderwerpen */
-  function applySidebarFilter() {
-    const input = document.getElementById("sidebarSearch");
-    const nav = document.getElementById("sidebarNav");
-    if (!input || !nav) return;
-    const q = (input.value || "").trim().toLowerCase();
+      wrap.appendChild(list);
+      sec.appendChild(wrap);
 
-    if (!q) {
-      $$(".nav-link", nav).forEach((a) => (a.style.display = ""));
-      $$(".nav-section", nav).forEach((s) => (s.style.display = ""));
-      removeNavEmpty(nav);
-      return;
-    }
+      /* beginstand: opgeslagen voorkeur, anders alleen het eerste
+         hoofdstuk open (nodigt uit om uit te klappen) */
+      const open = storedOpen ? storedOpen.indexOf(ch.id) !== -1 : ci === 0;
+      setChapterOpen(sec, open, false);
 
-    let visible = 0;
-    $$(".nav-link", nav).forEach((a) => {
-      const show = (a.dataset.search || "").indexOf(q) !== -1;
-      a.style.display = show ? "" : "none";
-      if (show) visible++;
-    });
-    $$(".nav-section", nav).forEach((s) => {
-      const any = $$(".nav-link", s).some((a) => a.style.display !== "none");
-      s.style.display = any ? "" : "none";
+      nav.appendChild(sec);
     });
-    removeNavEmpty(nav);
-    if (visible === 0) {
-      const p = el("div", "nav-empty", escapeHtml(t("empty_row")));
-      nav.appendChild(p);
-    }
-  }
-
-  function removeNavEmpty(nav) {
-    const p = $(".nav-empty", nav);
-    if (p) p.remove();
   }
 
   function updateSidebarActive(r) {
@@ -152,36 +179,275 @@ window.MLP = window.MLP || {};
       const isHome = a.dataset.nav === "home" && r.name === "home";
       const isTopic = a.dataset.topic && a.dataset.topic === r.id;
       a.classList.toggle("active", isHome || isTopic);
+      /* het hoofdstuk van het actieve onderwerp klapt vanzelf open */
+      if (isTopic) {
+        const sec = a.closest(".nav-section");
+        if (sec && sec.classList.contains("collapsed")) setChapterOpen(sec, true, true);
+      }
     });
   }
 
-  /* Mobiel: menu openen/sluiten */
+  /* ============================================================
+     Zijbalk inklappen — desktop (breedte 0) + mobiel (drawer)
+     ============================================================ */
+
+  function setSidebarCollapsed(collapsed) {
+    document.body.classList.toggle("sb-collapsed", collapsed);
+    try {
+      localStorage.setItem(SB_KEY, collapsed ? "1" : "0");
+    } catch (e) {
+      /* voorkeur onthouden is optioneel */
+    }
+  }
+
   function initMenu() {
     const btn = document.getElementById("menuToggle");
     const sb = document.getElementById("sidebar");
     const bd = document.getElementById("sidebarBackdrop");
     if (!btn || !sb || !bd) return;
 
-    const close = () => {
+    const mq = window.matchMedia("(max-width: 920px)");
+    const isMobile = () => mq.matches;
+
+    const closeDrawer = () => {
       sb.classList.remove("open");
       bd.hidden = true;
       btn.setAttribute("aria-expanded", "false");
       document.body.classList.remove("nav-open");
     };
-    const open = () => {
+    const openDrawer = () => {
       sb.classList.add("open");
       bd.hidden = false;
       btn.setAttribute("aria-expanded", "true");
       document.body.classList.add("nav-open");
     };
 
-    btn.addEventListener("click", () => (sb.classList.contains("open") ? close() : open()));
-    bd.addEventListener("click", close);
+    btn.addEventListener("click", () => {
+      if (isMobile()) {
+        if (sb.classList.contains("open")) closeDrawer();
+        else openDrawer();
+      } else {
+        setSidebarCollapsed(!document.body.classList.contains("sb-collapsed"));
+      }
+    });
+    bd.addEventListener("click", closeDrawer);
     sb.addEventListener("click", (ev) => {
-      if (ev.target.closest("a")) close();
+      if (ev.target.closest("a")) closeDrawer();
     });
     window.addEventListener("keydown", (ev) => {
-      if (ev.key === "Escape" && sb.classList.contains("open")) close();
+      if (ev.key === "Escape" && sb.classList.contains("open")) closeDrawer();
+    });
+
+    /* beginstand (desktop): eerder ingeklapt? */
+    if (!isMobile()) {
+      try {
+        if (localStorage.getItem(SB_KEY) === "1") document.body.classList.add("sb-collapsed");
+      } catch (e) {
+        /* negeer */
+      }
+    }
+
+    /* bij formaatwissel (desktop ↔ mobiel) de juiste modus herstellen */
+    const onMq = () => {
+      closeDrawer();
+      if (isMobile()) {
+        document.body.classList.remove("sb-collapsed");
+      } else {
+        try {
+          document.body.classList.toggle("sb-collapsed", localStorage.getItem(SB_KEY) === "1");
+        } catch (e) {
+          /* negeer */
+        }
+      }
+    };
+    if (mq.addEventListener) mq.addEventListener("change", onMq);
+    else if (mq.addListener) mq.addListener(onMq);
+  }
+
+  /* ============================================================
+     Zoeken — doorzoekt titels ÉN de volledige inhoud van alle
+     sub-topics (tekst, formules, code, tabellen, plotlabels).
+     De inhoudsindex wordt na het opstarten op de achtergrond
+     gebouwd; tot die tijd zoekt de balk op titel/samenvatting.
+     ============================================================ */
+
+  let searchIndex = null; /* null = nog aan het laden */
+  let searchIndexBuilding = false;
+  let searchTimer = null;
+
+  function stripHtml(html) {
+    const d = document.createElement("div");
+    d.innerHTML = html || "";
+    return (d.textContent || "").replace(/\s+/g, " ");
+  }
+
+  /* alle doorzoekbare tekst van één onderwerp verzamelen */
+  function topicSearchText(topic, tp) {
+    const parts = [
+      topic.title || tp.title || "",
+      tp.summary || topic.summary || "",
+      tp.keywords || topic.keywords || "",
+      topic.intro || "",
+      tp._chapterName || "",
+    ];
+    (topic.sections || []).forEach((sec) => {
+      if (sec.title) parts.push(sec.title);
+      (sec.blocks || []).forEach((b) => {
+        if (b.type === "text" || b.type === "callout") parts.push(stripHtml(b.html));
+        else if (b.type === "formula") parts.push(b.label || "", b.latex || "", b.caption || "");
+        else if (b.type === "code") parts.push(b.caption || "", b.source || "");
+        else if (b.type === "table") {
+          parts.push((b.headers || []).join(" "));
+          (b.rows || []).forEach((r) => parts.push((r || []).join(" ")));
+        } else if (b.type === "plot") {
+          parts.push(b.title || "", b.subtitle || "");
+          (b.curves || []).forEach((c) => parts.push(c.label || ""));
+          (b.points || []).forEach((p) => parts.push(p.label || ""));
+          (b.sliders || (b.slider ? [b.slider] : [])).forEach((s) => parts.push(s.label || ""));
+          (b.metrics || []).forEach((m) => parts.push(m.label || ""));
+        }
+      });
+    });
+    return parts.filter(Boolean).join(" ").replace(/\s+/g, " ").trim();
+  }
+
+  function buildSearchIndex() {
+    if (searchIndex || searchIndexBuilding) return;
+    searchIndexBuilding = true;
+
+    (async () => {
+      const index = await MLP.data.getIndex();
+      const chapters = index.chapters || [];
+      const chapterName = {};
+      chapters.forEach((c) => (chapterName[c.id] = c.name));
+
+      const entries = await Promise.all(
+        (index.topics || []).map(async (tp) => {
+          let text =
+            tp.title + " " + (tp.summary || "") + " " + (tp.keywords || "") + " " + (chapterName[tp.chapter] || "");
+          try {
+            const topic = await MLP.data.getTopic(tp.id);
+            text = topicSearchText(topic, {
+              title: tp.title,
+              summary: tp.summary,
+              keywords: tp.keywords,
+              _chapterName: chapterName[tp.chapter] || "",
+            });
+          } catch (e) {
+            /* onderwerp niet beschikbaar → val terug op de index-tekst */
+          }
+          return {
+            id: tp.id,
+            title: tp.title,
+            chapter: chapterName[tp.chapter] || "",
+            plain: text,
+            lower: text.toLowerCase(),
+          };
+        })
+      );
+
+      searchIndex = entries;
+      searchIndexBuilding = false;
+      /* filter opnieuw toepassen: nu met de volledige inhoud */
+      applySidebarFilter();
+    })().catch(() => {
+      searchIndexBuilding = false;
+    });
+  }
+
+  /* snippet rond de eerste treffer, met alle zoektermen gemarkeerd */
+  function snippetText(entry, terms) {
+    const text = entry.plain || "";
+    if (!text) return "";
+    const lower = entry.lower || text.toLowerCase();
+
+    let idx = -1;
+    for (const term of terms) {
+      const i = lower.indexOf(term);
+      if (i !== -1) {
+        idx = i;
+        break;
+      }
+    }
+    if (idx === -1) return "";
+
+    const PAD = 46;
+    let start = Math.max(0, idx - PAD);
+    if (start > 0) {
+      const sp = text.indexOf(" ", start);
+      if (sp !== -1 && sp < idx) start = sp + 1;
+    }
+    let end = Math.min(text.length, idx + 110);
+    if (end < text.length) {
+      const sp = text.lastIndexOf(" ", end);
+      if (sp !== -1 && sp > idx) end = sp;
+    }
+
+    const snippet =
+      (start > 0 ? "\u2026 " : "") + text.slice(start, end) + (end < text.length ? " \u2026" : "");
+
+    let safe = escapeHtml(snippet);
+    terms.forEach((term) => {
+      if (!term) return;
+      const rx = new RegExp("(" + term.replace(/[.*+?^${}()|[\]\\]/g, "\\$&") + ")", "gi");
+      safe = safe.replace(rx, "<mark>$1</mark>");
+    });
+    return safe;
+  }
+
+  function applySidebarFilter() {
+    const input = document.getElementById("sidebarSearch");
+    const sidebar = document.getElementById("sidebar");
+    const status = document.getElementById("searchStatus");
+    const results = document.getElementById("searchResults");
+    if (!input || !sidebar || !status || !results) return;
+
+    const q = (input.value || "").trim().toLowerCase();
+
+    if (!q) {
+      sidebar.classList.remove("searching");
+      status.hidden = true;
+      results.innerHTML = "";
+      return;
+    }
+
+    sidebar.classList.add("searching");
+
+    const terms = q.split(/\s+/).filter(Boolean);
+
+    /* volledige inhoud indien beschikbaar, anders titel/samenvatting */
+    let entries;
+    if (searchIndex) {
+      entries = searchIndex.map((e) => ({ id: e.id, title: e.title, chapter: e.chapter, plain: e.plain, lower: e.lower }));
+    } else {
+      entries = $$(".nav-link").map((a) => ({
+        id: a.dataset.topic,
+        title: a.textContent,
+        chapter: "",
+        plain: "",
+        lower: a.dataset.search || "",
+      }));
+    }
+
+    const hits = entries.filter((e) => terms.every((term) => e.lower.indexOf(term) !== -1));
+
+    status.hidden = false;
+    if (!hits.length) {
+      status.textContent = t("empty_row");
+      results.innerHTML = "";
+      return;
+    }
+    status.textContent = tf("search_results", { n: hits.length }) + (searchIndex ? "" : " \u00b7 " + t("search_indexing"));
+
+    results.innerHTML = "";
+    hits.slice(0, 30).forEach((h) => {
+      const a = el("a", "sr-item");
+      a.href = "#/onderwerp/" + encodeURIComponent(h.id);
+      a.appendChild(el("span", "sr-title", escapeHtml(h.title)));
+      if (h.chapter) a.appendChild(el("span", "sr-chapter", escapeHtml(h.chapter)));
+      const snippet = h.plain ? snippetText(h, terms) : "";
+      if (snippet) a.appendChild(el("span", "sr-snippet", snippet));
+      results.appendChild(a);
     });
   }
 
@@ -189,7 +455,38 @@ window.MLP = window.MLP || {};
     const input = document.getElementById("sidebarSearch");
     if (!input) return;
     input.value = "";
-    input.addEventListener("input", applySidebarFilter);
+
+    input.addEventListener("input", () => {
+      clearTimeout(searchTimer);
+      searchTimer = setTimeout(applySidebarFilter, 130);
+    });
+
+    input.addEventListener("keydown", (ev) => {
+      if (ev.key === "Escape" && input.value) {
+        input.value = "";
+        applySidebarFilter();
+        input.blur();
+      }
+    });
+
+    /* "/" springt naar het zoekveld (wiki-sneltoets) */
+    window.addEventListener("keydown", (ev) => {
+      if (ev.key !== "/" || ev.ctrlKey || ev.metaKey || ev.altKey) return;
+      const active = document.activeElement;
+      const tag = active ? active.tagName : "";
+      if (tag === "INPUT" || tag === "TEXTAREA" || tag === "SELECT" || (active && active.isContentEditable)) return;
+      ev.preventDefault();
+      /* zijbalk even tonen als die verborgen is */
+      if (window.matchMedia("(max-width: 920px)").matches) {
+        const sb = document.getElementById("sidebar");
+        const btn = document.getElementById("menuToggle");
+        if (sb && btn && !sb.classList.contains("open")) btn.click();
+      } else if (document.body.classList.contains("sb-collapsed")) {
+        setSidebarCollapsed(false);
+      }
+      input.focus();
+      input.select();
+    });
   }
 
   /* ============================================================
@@ -446,6 +743,13 @@ window.MLP = window.MLP || {};
       sidebarBuilt = false;
       const nav = document.getElementById("sidebarNav");
       if (nav) nav.innerHTML = "";
+
+      /* zoekindex opnieuw opbouwen in de nieuwe taal */
+      searchIndex = null;
+      searchIndexBuilding = false;
+      const results = document.getElementById("searchResults");
+      if (results) results.innerHTML = "";
+
       currentRoute = null;
       updateLangToggle();
       route();
diff --git a/js/blocks.js b/js/blocks.js
index cf99130..dc7edd9 100644
--- a/js/blocks.js
+++ b/js/blocks.js
@@ -309,8 +309,8 @@ window.MLP = window.MLP || {};
       const sx = (x) => PAD.l + ((x - xr[0]) / (xr[1] - xr[0])) * (W - PAD.l - PAD.r);
       const sy = (y) => H - PAD.b - ((y - yr[0]) / (yr[1] - yr[0])) * (H - PAD.t - PAD.b);
 
-      /* raster */
-      const gridG = svgNode("g", { stroke: "rgba(31,35,40,0.08)", "stroke-width": 1 });
+      /* raster — kleur via CSS-klasse, volgt het thema */
+      const gridG = svgNode("g", { class: "plot-grid", "stroke-width": 1 });
       const niceStep = (range) => {
         const raw = range / 6;
         const pow = Math.pow(10, Math.floor(Math.log10(raw)));
@@ -332,7 +332,7 @@ window.MLP = window.MLP || {};
       svg.appendChild(gridG);
 
       /* assen */
-      const axisAttrs = { stroke: "rgba(31,35,40,0.45)", "stroke-width": 1.2 };
+      const axisAttrs = { class: "plot-axis" };
       if (yr[0] <= 0 && yr[1] >= 0) {
         svg.appendChild(svgNode("line", Object.assign({ x1: PAD.l, y1: sy(0), x2: W - PAD.r, y2: sy(0) }, axisAttrs)));
       }
@@ -340,8 +340,8 @@ window.MLP = window.MLP || {};
         svg.appendChild(svgNode("line", Object.assign({ x1: sx(0), y1: PAD.t, x2: sx(0), y2: H - PAD.b }, axisAttrs)));
       }
 
-      /* ticklabels */
-      const tickAttrs = { fill: "rgba(31,35,40,0.65)", "font-size": 11, "font-family": "ui-monospace, SFMono-Regular, Menlo, Consolas, monospace" };
+      /* ticklabels — vulkleur via CSS-klasse (thema) */
+      const tickAttrs = { class: "plot-tick", "font-size": 11, "font-family": "ui-monospace, SFMono-Regular, Menlo, Consolas, monospace" };
       for (let gx = Math.ceil(xr[0] / xstep) * xstep; gx <= xr[1] + 1e-9; gx += xstep) {
         const tk = svgNode("text", Object.assign({ x: sx(gx), y: H - PAD.b + 18, "text-anchor": "middle" }, tickAttrs));
         tk.textContent = fmt(gx);
@@ -354,7 +354,7 @@ window.MLP = window.MLP || {};
       }
 
       /* asnamen */
-      const xa = svgNode("text", Object.assign({ x: W - PAD.r, y: H - PAD.b + 34, "text-anchor": "end", fill: "rgba(31,35,40,0.7)", "font-size": 11, "font-family": "-apple-system, BlinkMacSystemFont, Segoe UI, sans-serif" }, {}));
+      const xa = svgNode("text", Object.assign({ x: W - PAD.r, y: H - PAD.b + 34, "text-anchor": "end", class: "plot-tick", "font-size": 11, "font-family": "Lexend, -apple-system, Segoe UI, sans-serif" }, {}));
       xa.textContent = spec.xlabel || t("xlabel_fallback");
       svg.appendChild(xa);
 
@@ -376,9 +376,11 @@ window.MLP = window.MLP || {};
           pen = true;
         }
         if (d) {
-          svg.appendChild(
-            svgNode("path", { d: d, fill: "none", stroke: c.color, "stroke-width": 2.4, "stroke-linecap": "round", "stroke-linejoin": "round" })
-          );
+          /* stroke via inline style: CSS-variabelen (thema) werken
+             in styles, niet in SVG-attributen */
+          const path = svgNode("path", { d: d, fill: "none", "stroke-width": 2.4, "stroke-linecap": "round", "stroke-linejoin": "round" });
+          path.style.stroke = c.color;
+          svg.appendChild(path);
         }
       });
 
@@ -399,10 +401,10 @@ window.MLP = window.MLP || {};
             cx: sx(mx).toFixed(1),
             cy: sy(my).toFixed(1),
             r: 5.5,
-            fill: COLORS[mk.color] || mk.color || "#0969da",
-            stroke: "#ffffff",
-            "stroke-width": 1.4,
           });
+          dot.style.fill = COLORS[mk.color] || mk.color || "var(--cyan)";
+          dot.style.stroke = "var(--plot-point-ring)";
+          dot.style.strokeWidth = "1.4";
           if (mk.label) {
             const tt = svgNode("title", {});
             tt.textContent = mk.label + " (" + +mx.toFixed(2) + ", " + +my.toFixed(2) + ")";
@@ -422,10 +424,10 @@ window.MLP = window.MLP || {};
           cx: sx(p.x).toFixed(1),
           cy: sy(p.y).toFixed(1),
           r: 4.2,
-          fill: p.color,
-          stroke: "#ffffff",
-          "stroke-width": 1.2,
         });
+        c.style.fill = p.color;
+        c.style.stroke = "var(--plot-point-ring)";
+        c.style.strokeWidth = "1.2";
         if (p.label) {
           const title = svgNode("title", {});
           title.textContent = p.label + " (" + +p.x.toFixed(2) + ", " + +p.y.toFixed(2) + ")";
@@ -435,7 +437,7 @@ window.MLP = window.MLP || {};
       });
 
       /* crosshair + hover */
-      const cross = svgNode("line", { x1: 0, y1: PAD.t, x2: 0, y2: H - PAD.b, stroke: "rgba(31,35,40,0.3)", "stroke-dasharray": "4 4", "stroke-width": 1, opacity: 0 });
+      const cross = svgNode("line", { class: "plot-cross", x1: 0, y1: PAD.t, x2: 0, y2: H - PAD.b, opacity: 0 });
       svg.appendChild(cross);
 
       const overlay = svgNode("rect", { x: PAD.l, y: PAD.t, width: W - PAD.l - PAD.r, height: H - PAD.t - PAD.b, fill: "transparent" });
diff --git a/js/content.js b/js/content.js
index 866fac0..fedf569 100644
--- a/js/content.js
+++ b/js/content.js
@@ -31,13 +31,16 @@ window.MLP = window.MLP || {};
     return node;
   };
 
-  /* kleuren per naam (synchroniseer met css/style.css — lichte thema's,
-     dezelfde namen als de hoofdstuk-accenten) */
+  /* kleuren per naam — verwijzen naar de CSS-variabelen van het
+     thema (css/style.css), zodat curven, punten en accenten
+     automatisch meewisselen tussen dark en light.
+     Let op: deze waarden worden via inline style gezet (blokken/
+     legendes) — CSS-variabelen werken daar, in SVG-attributen niet. */
   const COLORS = {
-    cyan: "#0969da",
-    yellow: "#9a6700",
-    green: "#1a7f37",
-    magenta: "#8250df",
+    cyan: "var(--cyan)",
+    yellow: "var(--yellow)",
+    green: "var(--green)",
+    magenta: "var(--magenta)",
   };
 
   MLP.util = { $, $$, escapeHtml, el, COLORS };
diff --git a/js/i18n.js b/js/i18n.js
index 7b76488..edc5413 100644
--- a/js/i18n.js
+++ b/js/i18n.js
@@ -1,9 +1,10 @@
 /* ============================================================
    MLP — i18n.js
-   Tweilingige interface (NL standaard · EN beschikbaar).
+   Tweilingige interface (NL standaard · EN secundair).
    - t(key) geeft de string in de actieve taal
    - setLang(lang) wisselt van taal en vuurt "mlp:langchange"
-   - keuze wordt onthouden in localStorage ("mlp-lang")
+   - keuze wordt onthouden in localStorage ("mlp-lang");
+     zonder opgeslagen voorkeur is Nederlands de default
    ============================================================ */
 
 window.MLP = window.MLP || {};
@@ -31,10 +32,14 @@ window.MLP = window.MLP || {};
         "Van de eerste basisbegrippen tot logistic regression en support vector machines met scikit-learn: " +
         "de theorie in begrijpelijke stukjes, met formules, voorbeelden en code die je direct in je browser kunt draaien.",
       home_meta: "{ch} hoofdstukken \u00b7 {n} onderwerpen \u00b7 formules & uitvoerbare code",
-      sidebar_search_placeholder: "Zoeken\u2026",
-      sidebar_search_aria: "Zoek in onderwerpen",
-      menu_aria: "Menu openen of sluiten",
+      sidebar_search_placeholder: "Zoek in inhoud\u2026",
+      sidebar_search_aria: "Zoek in onderwerpen en hun inhoud",
+      menu_aria: "Zijbalk openen of sluiten",
       empty_row: "Geen onderwerpen gevonden \u2014 probeer een ander zoekwoord.",
+      search_results: "{n} resultaten",
+      search_indexing: "Inhoud doorzoeken\u2026",
+      theme_to_light: "Schakel naar licht thema",
+      theme_to_dark: "Schakel naar donker thema",
       /* niveaus */
       level_1: "Basis",
       level_2: "Verdieping",
@@ -110,10 +115,14 @@ window.MLP = window.MLP || {};
         "From the first basic concepts to logistic regression and support vector machines with scikit-learn: " +
         "the theory in understandable chunks, with formulas, examples and code you can run right in your browser.",
       home_meta: "{ch} chapters \u00b7 {n} topics \u00b7 formulas & runnable code",
-      sidebar_search_placeholder: "Search\u2026",
-      sidebar_search_aria: "Search topics",
-      menu_aria: "Open or close menu",
+      sidebar_search_placeholder: "Search content\u2026",
+      sidebar_search_aria: "Search topics and their content",
+      menu_aria: "Open or close sidebar",
       empty_row: "No topics found \u2014 try another search term.",
+      search_results: "{n} results",
+      search_indexing: "Indexing content\u2026",
+      theme_to_light: "Switch to light theme",
+      theme_to_dark: "Switch to dark theme",
       /* niveaus */
       level_1: "Basic",
       level_2: "Intermediate",
@@ -179,6 +188,8 @@ window.MLP = window.MLP || {};
     en: { 1: STRINGS.en.level_1, 2: STRINGS.en.level_2, 3: STRINGS.en.level_3 },
   };
 
+  /* NL is de default: alleen een expliciet opgeslagen voorkeur
+     of de taal-schakelaar wijzigt dat (de browser-taal telt niet). */
   function detectLang() {
     try {
       var stored = localStorage.getItem(STORAGE_KEY);
@@ -186,8 +197,7 @@ window.MLP = window.MLP || {};
     } catch (e) {
       /* localStorage kan geblokkeerd zijn */
     }
-    var nav = (navigator.language || (navigator.languages && navigator.languages[0]) || "nl").toLowerCase();
-    return nav.indexOf("en") === 0 ? "en" : "nl";
+    return "nl";
   }
 
   var lang = detectLang();
diff --git a/js/theme.js b/js/theme.js
new file mode 100644
index 0000000..16f0cbd
--- /dev/null
+++ b/js/theme.js
@@ -0,0 +1,78 @@
+/* ============================================================
+   MLP — theme.js
+   Dark/light-schakelaar.
+   - Donker is de default (zoals het origineel: #161616 + pastel)
+   - Keuze wordt onthouden in localStorage ("mlp-theme")
+   - js/app.js kan meeluisteren via "mlp:themechange"
+   De knop toont het icoon van de modus waar je NAARTOE schakelt.
+   ============================================================ */
+
+window.MLP = window.MLP || {};
+
+(function (MLP) {
+  "use strict";
+
+  var STORAGE_KEY = "mlp-theme";
+  var THEME_COLORS = { dark: "#161616", light: "#ffffff" };
+
+  function current() {
+    return document.documentElement.getAttribute("data-theme") === "light" ? "light" : "dark";
+  }
+
+  function apply(theme) {
+    document.documentElement.setAttribute("data-theme", theme);
+    var meta = document.querySelector('meta[name="theme-color"]');
+    if (meta) meta.setAttribute("content", THEME_COLORS[theme] || "#161616");
+  }
+
+  function set(theme) {
+    if (theme !== "dark" && theme !== "light") return;
+    if (theme === current()) return;
+    try {
+      localStorage.setItem(STORAGE_KEY, theme);
+    } catch (e) {
+      /* voorkeur onthouden is optioneel */
+    }
+    apply(theme);
+    updateToggleButton();
+    window.dispatchEvent(new CustomEvent("mlp:themechange", { detail: { theme: theme } }));
+  }
+
+  function toggle() {
+    set(current() === "dark" ? "light" : "dark");
+  }
+
+  /* aria/titel van de knop — in de actieve taal indien i18n al geladen is */
+  function updateToggleButton() {
+    var btn = document.getElementById("themeToggle");
+    if (!btn) return;
+    var key = current() === "dark" ? "theme_to_light" : "theme_to_dark";
+    var label = key;
+    if (MLP.i18n && MLP.i18n.t) label = MLP.i18n.t(key);
+    btn.setAttribute("aria-label", label);
+    btn.title = label;
+  }
+
+  function initToggleButton() {
+    var btn = document.getElementById("themeToggle");
+    if (!btn) return;
+    btn.addEventListener("click", toggle);
+    updateToggleButton();
+
+    /* labels herberekenen zodra i18n (defer) klaar is of de taal wisselt */
+    window.addEventListener("mlp:langchange", updateToggleButton);
+    if (MLP.i18n && MLP.i18n.applyToDocument) updateToggleButton();
+  }
+
+  if (document.readyState === "loading") {
+    window.addEventListener("DOMContentLoaded", initToggleButton);
+  } else {
+    initToggleButton();
+  }
+
+  MLP.theme = {
+    current: current,
+    set: set,
+    toggle: toggle,
+  };
+})(window.MLP);
-- 
2.47.3

